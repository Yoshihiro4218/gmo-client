server:
  forward-headers-strategy: native

spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    driver-class-name: org.h2.Driver
#    username: user
#    password: password
  thymeleaf:
    prefix=classpath:/templates/:
  security:
    oauth2:
      client:
        registration:
          gmo:
            provider: gmo
            client-name: GMO Aozora Bank Client
            client-id: yOuRClineTId
#            client-secret: clientSecret
            redirect-uri-template: '{baseUrl}/login/oauth2/code/{registrationId}'
            authorization-grant-type: authorization_code
            scope:
              - openid
              - profile
              - email
              - offline_access
            response-type: code
        provider:
          gmo:
            issue-uri: https://api.gmo-aozora.com/ganb/api/simulator/oauth
#            authorizationUri: https://api.gmo-aozora.com/ganb/api/simulator/oauth/auth/v1/authorization
#            tokenUri: https://auth.login.yahoo.co.jp/yconnect/v2/token
#            jwkSetUri: https://auth.login.yahoo.co.jp/yconnect/v2/jwks
#            userInfoUri: https://userinfo.yahooapis.jp/yconnect/v2/attribute

################################
####   Custom Properties    ####
################################

auth2:
  post-logout-redirect-uri: http://localhost:8080/login
#  resource-server-uri
#  base-uri